package eu.faircode.xlua.api.xmock.call;

/*public class PutPropsStateCommand {
    //
    //
    //We can also have a table like prop settings ? make it easier ?????
    //(Prop name) => (settings) ? *then hide or skip depending if value is skip/hide*
    //else
    //
    //(Prop name) => (setting name)
    //(Setting name) => (value)
    //
}*/

